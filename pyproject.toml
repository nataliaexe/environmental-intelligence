[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"


[project]
name = "environmental-intelligence-simulator"
version = "0.1.0"
description = "Environmental simulation engine"
requires-python = ">=3.10"


[tool.setuptools.packages.find]
include = ["simulator*"]
